import * as React from 'react';
import { render } from 'react-dom';
import '../../Components/Styles/relax.css';
import Relax from './Relax';

const RelaxMain = () => {
  return (
    <div>
      <Relax></Relax>
    </div>
  );
};

export default RelaxMain;
